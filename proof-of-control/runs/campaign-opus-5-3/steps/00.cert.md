@[action:a1]{read invoices} is a %[kind]{db.read} on %[resource]{invoices}, and it reads nothing beyond the queue itself. I need the full list before I can judge which invoices are due and which payment stays within the spending limit, so this read comes first and moves no money. It requires no controls beyond the mandate it runs under, @[grant:g]{the grant}.